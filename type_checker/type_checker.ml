(* type_checker.ml *)
open Ppxlib

(* A dummy function to check types; currently incomplete *)
let check_type _program =
  print_endline "Type checking not fully implemented.";
  true (* Always returns true for now *)

